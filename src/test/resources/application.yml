spring:
  application:
    name: FlowControl
  datasource:
    url: jdbc:h2:mem:test
    username:
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
flowcontrol:
  admin:
    username: admin
    password: admin
